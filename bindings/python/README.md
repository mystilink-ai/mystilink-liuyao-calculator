# Python binding

The primary Python package is the repository root package `mystilink_liuyao`
(install with `python3 -m pip install -e .` from the repo root).

This directory re-exports the same API for language-matrix layout consistency.

```python
from mystilink_liuyao_binding import cast_lines
```
